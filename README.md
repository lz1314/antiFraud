# Process + SpringBoot example

## Attention
原生启动程序要先打包，才会按bpmn发布restful接口形式，目前已经屏蔽自动生成接口服务
clean package -Dmaven.test.skip=true -Denforcer.skip=true -Dformatter.skip=true -Dimpsort.skip=true -f pom.xml

